.codex
eph

# Fetch-on-demand / bring-your-own force field assets -- no longer
# git-vendored (see .praxia/docs/specs/260824_license-provenance-verification.md).
# Populated locally only by scripts/sync_forcefields.py, for regenerating
# src/proxide/assets/_asset_index.py -- never meant to be committed.
src/proxide/assets/amber/
src/proxide/assets/water/
src/proxide/assets/implicit/
src/proxide/assets/openmm_bundled/
src/proxide/assets/charmm/
src/proxide/assets/protein.ff14SB.xml
src/proxide/assets/protein.ff19SB.xml

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv/
pip-log.txt
pip-delete-this-directory.txt
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.log
.git
.mypy_cache
.pytest_cache
.ruff_cache
.hypothesize

# Build
build/
dist/
*.egg-info/
*.egg

# Jupyter
.ipynb_checkpoints

.DS_Store

# IDEs
.vscode/
.idea/
*.swp
.gemini/

# Data
rcsb_data/
mdcath_data/
data/rotlibs/
SimpleOpt*.zip
ExtendedOpt*.zip

# Rust / Maturin
/target
/oxidize/target
*.so
*.dylib
*.dll
target/

# Compiled extensions (maturin build artifacts)
*.abi3.so
cluster-config.toml

# Caches
.fastembed_cache/
.pdbfixer_upstream/

# Praxia agent state
# .praxia/ tracked (daily log, docs, handoffs, backlog)
.praxia/cache/
.praxia/eph/
praxia.toml

# Coverage reports
scripts/coverage/cobertura.xml

# MASTER-derived data — CC-BY-NC-SA 4.0; NOT for redistribution
*.master.pb.zst

# bathos per-run artifacts (catalog itself lives at ~/.bth/catalog, not here)
outputs/
scripts/**/*.bth.lock.toml
scripts/slurm/_bth_env.sh
scripts/scratch/
